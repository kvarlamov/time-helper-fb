Main Functional
#Main page
    -AUTH:
        - Add validations to login/ registration pages
    - FIREBASE DATABASE ADD 
        - SAVE TO DATABASE
        - UPDATE database task (add button edit for selected - всплывающее окно - ок - сохраняем, отмена - обнуляем поля)
            await firebase.datebase().ref('targets').child(id).update({
                title,
                targetTime
            }) // далее через мутацию передаем и методом find по id ищем нужное
        - DELETE database task
            await firebase().ref('tasks').child(id).remove()
             -- мутация 
    - get/ save setting to DATABASE
    - ADd validation on time form before creation!!!    
        
    - 90 degrees rotate on logo
#Settings page
    - 
#About
    - write rules of using timer

#Bugs
    - hover перестает работать после первого клика
